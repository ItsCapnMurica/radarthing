// Non-secret app config. Your ORS API key is NOT stored here —
// it's entered once in the browser and saved to localStorage on your device only.
window.APP_CONFIG = {
  defaultCenter: [39.4143, -77.4105], // Frederick, MD
  defaultZoom: 12,
  orsKeyStorageName: "driveradar_ors_key"
};
